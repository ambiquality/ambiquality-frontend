# `catalog-browse/` (Phase 6 — F11–F14)

Visitor browse / search / filter / paginate of public entities (buildings → rooms →
sensors). Reads Public.Api. Note pagination is heterogeneous: catalog lists are offset
(`page`/`pageSize`); observations are keyset/cursor (`next` link).
